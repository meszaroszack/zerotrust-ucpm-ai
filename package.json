{
  "name": "zerotrust-ai",
  "version": "1.0.0",
  "description": "ZEROTRUST AI — AI-native OneTrust UCPM implementation cockpit",
  "scripts": {
    "start": "node backend/src/index.js",
    "install:all": "cd backend && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
