// OpenAI Provider — scaffolded for future activation
async function complete({ messages, systemPrompt, apiKey, model }) {
  throw new Error('OpenAI provider is not yet active. Configure your API key in Settings to enable it.');
}
module.exports = { complete };
