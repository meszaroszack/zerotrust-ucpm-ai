// Azure OpenAI Provider — scaffolded for future activation
async function complete({ messages, systemPrompt, apiKey, model }) {
  throw new Error('Azure OpenAI provider is not yet active. Configure your credentials in Settings to enable it.');
}
module.exports = { complete };
